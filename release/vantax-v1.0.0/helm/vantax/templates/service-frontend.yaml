apiVersion: v1
kind: Service
metadata:
  name: {{ include "vantax.fullname" . }}-frontend
  labels:
    {{- include "vantax.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "vantax.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
