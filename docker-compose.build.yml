# Local source build overlay — use instead of pulling from GHCR
# Usage: docker compose -f docker-compose.yml -f docker-compose.build.yml build

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api

  worker:
    build:
      context: .
      dockerfile: Dockerfile.api

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
